## 2024-04-14 - Add ARIA Labels to Icon-only Buttons
**Learning:** Icon-only buttons in interactive components like ChatComposer need accessible labels for screen readers.
**Action:** Always verify icon-only interactive elements have `aria-label` and `title` attributes.
