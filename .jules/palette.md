## 2024-05-14 - Tooltips and ARIA Labels
**Learning:** Added tooltips and ARIA labels for accessibility to the main Chat Composer interface. Using both attributes greatly improves both screen reader experience and regular mouse interaction (via tooltips) for icon-only buttons.
**Action:** Always pair `aria-label` and `title` when creating icon-only buttons in complex UI elements like chat input bars.
