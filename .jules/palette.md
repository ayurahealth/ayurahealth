## 2024-05-18 - Added ARIA labels to chat composer buttons
**Learning:** React state variables (like `isListening`) can be used to dynamically set `aria-label` and `title` attributes on icon buttons.
**Action:** Always check if icon buttons have semantic text alternatives, and make sure dynamic buttons (like start/stop recording) have dynamic labels that reflect their current state.
