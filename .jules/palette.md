## 2025-04-20 - Icon-Only Buttons Accessibility
**Learning:** Icon-only buttons (like `Remove Attachment` or `Voice Input`) are prevalent in modern UI but completely inaccessible without proper ARIA attributes.
**Action:** Always add `aria-label` and `title` to icon-only buttons to ensure they can be understood by screen readers and identified on hover by visual users.
