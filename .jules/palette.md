## 2025-05-18 - Missing ARIA Labels on Interactive Elements
**Learning:** Icon-only interactive elements (like buttons) must include descriptive 'aria-label' and 'title' attributes to be accessible. A number of components in the project are missing these attributes.
**Action:** Always ensure that any icon-only button or interactive element receives a descriptive `aria-label` attribute and `title` attribute.
